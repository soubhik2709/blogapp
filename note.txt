
i have to make -->
landingpage , create blog ,show post, account, public   Name- blogApp
blog page, every blog post is showing , public page.    Name -userblogs
blog create , every blog is create , private page.      Name - blogCreate


Todo-->
1.use redis

2.use aggregation to query the blog data from doubt
db.users.aggregate([
  {
    $lookup: {
      from: "blogs",         // The collection you want to get data from
      localField: "_id",     // The ID in your User collection
      foreignField: "userId",// The field in Blog collection that stores User ID
      as: "userBlogs"        // Name for the combined result field
    }
  }
]);

3. use all the features of postman to learn postman.



doubt-->
1.how to use populate in mongodb to get all the user blog?
2.what is the difference between formdata and json?
3.when use this different url-->/showblog?userId=69804bab... vs /showblog/:userId: , when to use which one 

After logout the accessToken is valid untill its expire, so i can get access the protected route. so solution is 
🔴 Option 1: Access-token blacklist (rare)
Store access token IDs (jti) in DB/Redis
On logout, blacklist it
Middleware checks blacklist
❌ Heavy
❌ Slower
❌ Rarely worth it


NextTask-->08022026

Learn while adding:
Pagination
Filtering
Sorting
Search

What concepts you’ll learn
Query optimization
Indexing
Query composition
Avoiding overfetching